{
  "git": {
    "deploymentEnabled": false
  },
  "buildCommand": "npm run db:migrate && next build",
  "crons": [
    {
      "path": "/api/tracking/sync",
      "schedule": "0 4 * * *"
    }
  ]
}
